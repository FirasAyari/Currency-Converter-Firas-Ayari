<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.firasayari.currencyconverter">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logocc"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logocc"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReadSqliteDataActivity"></activity>
        <activity android:name=".ReadSqliteDataActivity.java" />
        <activity android:name=".Login.RegistrationActivity" />
        <activity android:name=".Login.SplashScrActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".view.OperationsAdapter" />
        <activity android:name=".Login.PWresetActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>